import * as React from 'react';

interface IAppProps {
}

const AboutPage: React.FunctionComponent<IAppProps> = (props) => {
  return(
    <>
    <h2>
    About Page
    </h2>
    </>
  )
};

export default AboutPage;
