import * as React from 'react';

interface IAppProps {
}

const TestPage: React.FunctionComponent<IAppProps> = (props) => {
  return(
    <>
    <h2>
    Test Page
    </h2>
    </>
  )
};

export default TestPage;
